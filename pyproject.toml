[project]
name = "stocksense"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "khushi mahida", email = "the.khushi.mahida@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "scrapy>=2.19.0",
]

[project.scripts]
stocksense = "stocksense:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
